# Python Scripts

Python scripts for data processing, EDA, and model training tasks.

- `util/text_similarity.py`: in-memory fuzzy and semantic text ranking for `list[str]` and `pandas.Series` inputs.
- Python dependencies for the similarity utility: `rapidfuzz`, `sentence-transformers`, `scikit-learn`, `pandas`, `numpy`.
